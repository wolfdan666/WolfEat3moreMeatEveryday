// CPP program to illustrate
// gcd function of C++ STL
#include <iostream>
#include <algorithm>

using namespace std;

int main()
{
    cout << "gcd(0, 20) = " << __gcd(0, 20) << endl; // 20! 所以小白月赛D题要自己特判,以后这种就不用自己写了，效率太低
}